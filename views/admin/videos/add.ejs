<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload New Reel</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        }

        body {
            display: flex;
            min-height: 100vh;
            background: #f5f7fa;
        }

        /* Sidebar Styles (same as dashboard) */
        .sidebar {
            width: 260px;
            background: white;
            box-shadow: 2px 0 15px rgba(0, 0, 0, 0.05);
            padding: 30px 0;
            position: fixed;
            height: 100vh;
            z-index: 100;
            transition: width 0.3s ease;
        }

        .logo {
            padding: 0 25px 30px;
            border-bottom: 1px solid #eef2f7;
            margin-bottom: 30px;
        }

        .logo h2 {
            color: #1a1a1a;
            font-size: 22px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo h2::before {
            content: "⚡";
            font-size: 24px;
        }

        .nav-menu {
            padding: 0 20px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 14px 20px;
            margin: 8px 0;
            border-radius: 12px;
            color: #64748b;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .nav-item:hover {
            background: #f1f5f9;
            color: #1e3c72;
        }

        .nav-item.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
        }

        .nav-item i {
            font-size: 20px;
        }

        .nav-divider {
            height: 1px;
            background: #eef2f7;
            margin: 25px 0;
        }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 14px 20px;
            margin: 8px 20px;
            border-radius: 12px;
            color: #ef4444;
            text-decoration: none;
            font-weight: 500;
            background: #fef2f2;
            border: none;
            width: calc(100% - 40px);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .logout-btn:hover {
            background: #fee2e2;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 260px;
            padding: 30px;
            transition: margin-left 0.3s ease;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Upload Form Styles */
        .upload-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 45px 40px;
            width: 100%;
            max-width: 720px;
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        h2 {
            text-align: center;
            color: #1e3c72;
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .subtitle {
            text-align: center;
            color: #64748b;
            margin-bottom: 35px;
            font-size: 16px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #1e3c72;
            font-weight: 600;
            font-size: 16px;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 14px 18px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            background: #f9fafb;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        input:focus,
        textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.15);
            background: white;
        }

        textarea {
            min-height: 160px;
            resize: vertical;
            font-family: inherit;
        }

        .image-upload-area {
            border: 3px dashed #cbd5e0;
            border-radius: 12px;
            padding: 35px;
            text-align: center;
            background: #f9fafb;
            cursor: pointer;
            transition: all 0.3s ease;
            margin: 15px 0;
        }

        .image-upload-area:hover {
            border-color: #667eea;
            background: #f1f5f9;
        }

        .image-upload-area input {
            display: none;
        }

        .upload-icon {
            font-size: 48px;
            color: #667eea;
            margin-bottom: 15px;
        }

        .upload-text {
            font-size: 18px;
            color: #4a5568;
            margin-bottom: 8px;
            font-weight: 500;
        }

        .upload-hint {
            font-size: 14px;
            color: #718096;
        }

        .image-preview {
            margin-top: 20px;
            text-align: center;
            display: none;
        }

        .image-preview img {
            max-width: 100%;
            max-height: 280px;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        }

        .error-message {
            background: rgba(239, 68, 68, 0.15);
            color: #ef4444;
            padding: 14px 18px;
            border-radius: 12px;
            margin-bottom: 25px;
            text-align: center;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        .submit-btn {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.3);
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 30px;
            color: #718096;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #4facfe;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar {
                width: 80px;
            }

            .logo h2 span,
            .nav-item span,
            .logout-btn span {
                display: none;
            }

            .nav-item {
                justify-content: center;
            }

            .main-content {
                margin-left: 80px;
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }

            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>

<body>
    <!-- Sidebar (from your dashboard) -->
    <aside class="sidebar">
        <div class="logo">
            <h2>Admin Pro</h2>
        </div>

        <nav class="nav-menu">
            <a href="#reels" class="nav-item active">
                <i class="fas fa-film"></i>
                <span>Reels</span>
            </a>
            <a href="#blogs" class="nav-item">
                <i class="fas fa-blog"></i>
                <span>Blogs</span>
            </a>
            <a href="#contacts" class="nav-item">
                <i class="fas fa-envelope"></i>
                <span>Contacts</span>
            </a>
            <a href="#demo-requests" class="nav-item">
                <i class="fas fa-phone-volume"></i>
                <span>Demo Requests</span>
            </a>

            <div class="nav-divider"></div>

            <a href="/admin/videos/add/reel" class="nav-item">
                <i class="fas fa-plus-circle"></i>
                <span>Add Reel</span>
            </a>
            <a href="/admin/blogs/add" class="nav-item">
                <i class="fas fa-edit"></i>
                <span>Add Blog</span>
            </a>
        </nav>

        <a href="/admin/logout" class="logout-btn">
            <i class="fas fa-sign-out-alt"></i>
            <span>Logout</span>
        </a>
    </aside>

    <!-- Main Content - Reel Upload Form -->
    <main class="main-content">
        <div class="upload-container">
            <h2>Upload New Reel</h2>
            <p class="subtitle">Add high-quality vertical content for social media</p>

            <% if (typeof error !=='undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
                <% } %>

                    <form action="/admin/videos/add/reel" method="POST" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="title">Reel Title *</label>
                            <input type="text" name="title" id="title" placeholder="Enter a catchy title" required
                                value="<%= typeof title !== 'undefined' ? title : '' %>">
                        </div>

                        <div class="form-group">
                            <label for="description">Description (Optional)</label>
                            <textarea name="description" id="description"
                                placeholder="Brief description of this reel..."><%= typeof description !== 'undefined' ? description : '' %></textarea>
                        </div>
                        <div class="form-group">
                            <label for="video">Select Reel File *</label>

                            <label for="video" class="image-upload-area"> <!-- Use label instead of div + onclick -->
                                <div class="upload-icon">📱</div>
                                <div class="upload-text">Click to select your reel</div>
                                <div class="upload-hint">
                                 Max 4.5MB • Recommended: 9:16 (vertical)
                                </div>

                                <!-- Hidden actual input -->
                                <input type="file" name="video" id="video" accept="video/*" required
                                    style="display: none;" onchange="checkFileSize(this)">
                            </label>

                            <!-- Optional: Show selected file name -->
                            <div id="fileName"
                                style="margin-top: 12px; color: #667eea; font-size: 15px; text-align: center;"></div>
                        </div>
                        <button type="submit" class="submit-btn" id="submitBtn">
                            <i class="fas fa-paper-plane"></i> Upload Reel
                        </button>
                    </form>

                    <a href="/admin/dashboard" class="back-link">← Back to Dashboard</a>
        </div>
    </main>

    <script>
        function checkFileSize(input) {
            const maxMB = 4.5;
            const maxSize = maxMB * 1024 * 1024;
            const submitBtn = document.getElementById('submitBtn');
            const fileNameDiv = document.getElementById('fileName');

            if (input.files && input.files[0]) {
                if (input.files[0].size > maxSize) {
                    alert('File size exceeds the Vercel 4.5MB limit. Please upload a smaller video.');
                    input.value = ''; // Clear the file
                    fileNameDiv.textContent = '';
                    submitBtn.disabled = true;
                    submitBtn.style.opacity = '0.5';
                    submitBtn.style.cursor = 'not-allowed';
                } else {
                    fileNameDiv.textContent = input.files[0].name;
                    submitBtn.disabled = false;
                    submitBtn.style.opacity = '1';
                    submitBtn.style.cursor = 'pointer';
                }
            }
        }
    </script>
</body>

</html>