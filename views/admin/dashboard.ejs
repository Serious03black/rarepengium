<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - rare penguin fashion</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #667eea;
            --primary-dark: #764ba2;
            --success: #10b981;
            --danger: #ef4444;
            --warning: #f59e0b;
            --text: #1e293b;
            --text-light: #64748b;
            --bg: #f8fafc;
            --card: #ffffff;
            --border: #e2e8f0;
            --shadow-sm: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            --shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
            --radius: 16px;
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--bg);
            color: var(--text);
            display: flex;
            min-height: 100vh;
            line-height: 1.6;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            background: white;
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.08);
            position: fixed;
            height: 100vh;
            z-index: 1000;
            padding: 30px 0;
            transition: var(--transition);
            overflow-y: auto;
        }

        .logo {
            padding: 0 30px 40px;
            border-bottom: 1px solid var(--border);
            margin-bottom: 30px;
            text-align: center;
        }

        .logo h2 {
            font-size: 26px;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .nav-menu {
            padding: 0 20px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 16px 20px;
            margin: 6px 10px;
            border-radius: 14px;
            color: var(--text-light);
            text-decoration: none;
            font-weight: 600;
            font-size: 15px;
            transition: var(--transition);
        }

        .nav-item i {
            font-size: 20px;
            width: 24px;
            text-align: center;
        }

        .nav-item:hover {
            background: #f1f5f9;
            color: var(--primary);
            transform: translateX(8px);
        }

        .nav-item.active {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .nav-item.active i {
            color: white;
        }

        .nav-divider {
            height: 1px;
            background: var(--border);
            margin: 30px 20px;
        }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 16px 20px;
            margin: 20px 20px 10px;
            border-radius: 14px;
            color: var(--danger);
            background: #fef2f2;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
        }

        .logout-btn:hover {
            background: #fee2e2;
            transform: translateX(8px);
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 280px;
            padding: 40px;
            transition: var(--transition);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 50px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border);
        }

        .header h1 {
            font-size: 32px;
            font-weight: 700;
            color: var(--text);
        }

        .date-display {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            padding: 12px 24px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 15px;
            box-shadow: var(--shadow);
        }

        /* Stats Cards */
        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 25px;
            margin-bottom: 50px;
        }

        .stat-card {
            background: white;
            padding: 30px;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            gap: 24px;
            transition: var(--transition);
        }

        .stat-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-lg);
        }

        .stat-icon {
            width: 80px;
            height: 80px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: white;
            box-shadow: var(--shadow);
        }

        .reel-stat .stat-icon {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .blog-stat .stat-icon {
            background: linear-gradient(135deg, #10b981, #059669);
        }

        .contact-stat .stat-icon {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
        }

        .demo-stat .stat-icon {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
        }

        .stat-info h3 {
            font-size: 40px;
            font-weight: 800;
            color: var(--text);
            margin-bottom: 4px;
        }

        .stat-info p {
            color: var(--text-light);
            font-weight: 600;
            font-size: 15px;
        }

        /* Content Sections */
        .content-section {
            background: white;
            border-radius: var(--radius);
            padding: 35px;
            margin-bottom: 40px;
            box-shadow: var(--shadow);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: var(--text);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .section-title i {
            color: var(--primary);
        }

        .badge {
            background: var(--primary);
            color: white;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            box-shadow: var(--shadow-sm);
        }

        .add-btn {
            padding: 12px 28px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: var(--transition);
            box-shadow: var(--shadow);
        }

        .add-btn:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-lg);
        }

        /* Grid */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 28px;
        }

        .item-card {
            background: white;
            border-radius: var(--radius);
            overflow: hidden;
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--border);
            transition: var(--transition);
        }

        .item-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }

        .thumbnail {
            position: relative;
            height: 200px;
            overflow: hidden;
            background: #000;
        }

        .thumbnail video,
        .thumbnail img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .item-card:hover .thumbnail video,
        .item-card:hover .thumbnail img {
            transform: scale(1.08);
        }

        .type-badge {
            position: absolute;
            top: 16px;
            right: 16px;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 8px 16px;
            border-radius: 50px;
            font-size: 13px;
            font-weight: 600;
            backdrop-filter: blur(10px);
        }

        .play-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 60px;
            color: rgba(255, 255, 255, 0.9);
            pointer-events: none;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .item-card:hover .play-icon {
            opacity: 1;
        }

        .info {
            padding: 24px;
        }

        .info h4 {
            margin: 0 0 12px;
            font-size: 18px;
            font-weight: 700;
            color: var(--text);
        }

        .info p {
            margin: 0 0 20px;
            color: var(--text-light);
            font-size: 15px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 20px;
            border-top: 1px solid var(--border);
        }

        .date {
            color: var(--text-light);
            font-size: 14px;
            font-weight: 500;
        }

        .delete-btn {
            color: var(--danger);
            background: #fef2f2;
            padding: 8px 16px;
            border-radius: 10px;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
        }

        .delete-btn:hover {
            background: #fee2e2;
        }

        .no-items {
            grid-column: 1 / -1;
            text-align: center;
            padding: 80px 30px;
            color: var(--text-light);
        }

        .no-items i {
            font-size: 70px;
            color: #cbd5e0;
            margin-bottom: 25px;
            display: block;
        }

        .no-items p {
            font-size: 18px;
            margin-bottom: 10px;
        }

        /* Contact & Demo Specific */
        .contact-card {
            padding: 30px;
        }

        .contact-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .contact-info h4 {
            font-size: 20px;
            margin-bottom: 6px;
        }

        .contact-info small {
            color: var(--text-light);
            font-size: 15px;
        }

        .subject-badge {
            background: #eef2ff;
            color: #4f46e5;
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
        }

        .meta-info {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin: 20px 0;
            font-size: 15px;
            color: var(--text-light);
        }

        .message-text {
            background: #f8fafc;
            padding: 20px;
            border-radius: 12px;
            border-left: 4px solid var(--primary);
            margin: 20px 0;
            font-size: 16px;
            line-height: 1.7;
        }

        /* Responsive */
        @media (max-width: 1200px) {
            .grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
        }

        @media (max-width: 1024px) {
            .sidebar {
                width: 80px;
                padding: 20px 0;
            }

            .logo h2, .nav-item span, .logout-btn span {
                display: none;
            }

            .nav-item {
                justify-content: center;
                padding: 16px;
            }

            .main-content {
                margin-left: 80px;
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .main-content {
                margin-left: 0;
                padding: 20px;
            }

            .stats-cards, .grid {
                grid-template-columns: 1fr;
            }

            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="logo">
            <h2>Admin Pro</h2>
        </div>

        <nav class="nav-menu">
            <a href="#reels" class="nav-item active">
                <i class="fas fa-film"></i>
                <span>Reels</span>
            </a>
            <a href="#blogs" class="nav-item">
                <i class="fas fa-blog"></i>
                <span>Blogs</span>
            </a>
            <a href="#contacts" class="nav-item">
                <i class="fas fa-envelope"></i>
                <span>Contacts</span>
            </a>
            <a href="#demo-requests" class="nav-item">
                <i class="fas fa-phone-volume"></i>
                <span>Demo Requests</span>
            </a>

            <div class="nav-divider"></div>

            <a href="/admin/videos/add/reel" class="nav-item">
                <i class="fas fa-plus-circle"></i>
                <span>Add Reel</span>
            </a>
            <a href="/admin/blogs/add" class="nav-item">
                <i class="fas fa-edit"></i>
                <span>Add Blog</span>
            </a>
        </nav>

        <a href="/admin/logout" class="logout-btn">
            <i class="fas fa-sign-out-alt"></i>
            <span>Logout</span>
        </a>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div class="header">
            <h1>Dashboard Overview</h1>
            <div class="date-display" id="current-date"></div>
        </div>

        <!-- Statistics Cards -->
        <div class="stats-cards">
            <div class="stat-card reel-stat">
                <div class="stat-icon">
                    <i class="fas fa-film"></i>
                </div>
                <div class="stat-info">
                    <h3><%= videos.filter(v => v.type === 'reel').length %></h3>
                    <p>Total Reels</p>
                </div>
            </div>

            <div class="stat-card blog-stat">
                <div class="stat-icon">
                    <i class="fas fa-blog"></i>
                </div>
                <div class="stat-info">
                    <h3><%= blogs ? blogs.length : 0 %></h3>
                    <p>Total Blogs</p>
                </div>
            </div>

            <div class="stat-card contact-stat">
                <div class="stat-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <div class="stat-info">
                    <h3><%= typeof contacts !== 'undefined' && contacts ? contacts.length : 0 %></h3>
                    <p>Contact Messages</p>
                </div>
            </div>

            <div class="stat-card demo-stat">
                <div class="stat-icon">
                    <i class="fas fa-phone-volume"></i>
                </div>
                <div class="stat-info">
                    <h3><%= demoRequests?.length || 0 %></h3>
                    <p>Demo Requests</p>
                </div>
            </div>
        </div>

        <!-- Reels Section -->
        <section id="reels" class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-film"></i> Reels
                </h2>
                <a href="/admin/videos/add/reel" class="add-btn">
                    <i class="fas fa-plus"></i> Add New Reel
                </a>
            </div>

            <div class="grid">
                <% if (videos.filter(v => v.type === 'reel').length > 0) { %>
                    <% videos.filter(v => v.type === 'reel').forEach(reel => { %>
                        <div class="item-card">
                            <div class="thumbnail">
                                <video muted preload="metadata">
                                    <source src="<%= reel.videoUrl %>?f_auto,q_auto" type="video/mp4">
                                </video>
                                <span class="type-badge">REEL</span>
                                <div class="play-icon">▶</div>
                            </div>
                            <div class="info">
                                <h4><%= reel.title %></h4>
                                <p><%= reel.description || 'No description' %></p>
                                <div class="card-footer">
                                    <span class="date">
                                        <%= new Date(reel.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                    </span>
                                    <a href="/admin/videos/delete/<%= reel._id %>" class="delete-btn"
                                        onclick="return confirm('Permanently delete this reel?')">
                                        Delete
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="no-items">
                        <i class="fas fa-film"></i>
                        <p>No reels uploaded yet</p>
                        <p>Click "Add New Reel" to upload your first one!</p>
                    </div>
                <% } %>
            </div>
        </section>

        <!-- Blogs Section -->
        <section id="blogs" class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-blog"></i> Blog Posts
                </h2>
                <a href="/admin/blogs/add" class="add-btn">
                    <i class="fas fa-plus"></i> Write New Blog
                </a>
            </div>

            <div class="grid">
                <% if (blogs && blogs.length > 0) { %>
                    <% blogs.forEach(blog => { %>
                        <div class="item-card">
                            <div class="thumbnail">
                                <% if (blog.imageUrl) { %>
                                    <img src="<%= blog.imageUrl %>?q=80&w=600" alt="<%= blog.title %>">
                                <% } else { %>
                                    <div style="background: linear-gradient(135deg, var(--primary), var(--primary-dark)); display: flex; align-items: center; justify-content: center; color: white; font-size: 60px;">
                                        <i class="fas fa-blog"></i>
                                    </div>
                                <% } %>
                            </div>
                            <div class="info">
                                <h4><%= blog.title %></h4>
                                <p><%= blog.paragraph1 ? blog.paragraph1.substring(0, 120) + '...' : 'No preview' %></p>
                                <div class="card-footer">
                                    <span class="date">
                                        <%= new Date(blog.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                    </span>
                                    <div>
                                        <a href="/admin/blogs/edit/<%= blog._id %>" style="color: var(--primary); margin-right: 20px; font-weight: 600; text-decoration: none;">
                                            Edit
                                        </a>
                                        <a href="/admin/blogs/delete/<%= blog._id %>" class="delete-btn"
                                            onclick="return confirm('Permanently delete this blog?')">
                                            Delete
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="no-items">
                        <i class="fas fa-blog"></i>
                        <p>No blog posts yet</p>
                        <p>Start sharing your thoughts and stories!</p>
                    </div>
                <% } %>
            </div>
        </section>

        <!-- Contact Messages -->
        <section id="contacts" class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-envelope"></i> Contact Messages
                </h2>
                <div class="badge">
                    <%= typeof contacts !== 'undefined' && contacts ? contacts.length : 0 %> messages
                </div>
            </div>

            <div class="grid" style="grid-template-columns: 1fr;">
                <% if (contacts && contacts.length > 0) { %>
                    <% contacts.forEach(contact => { %>
                        <div class="item-card contact-card">
                            <div class="contact-header">
                                <div class="contact-info">
                                    <h4><%= contact.name %></h4>
                                    <small>
                                        <%= contact.email %>
                                        <% if (contact.phone) { %> • <%= contact.phone %> <% } %>
                                    </small>
                                </div>
                                <span class="subject-badge"><%= contact.subject %></span>
                            </div>

                            <% if (contact.budget || contact.membership || contact.website) { %>
                                <div class="meta-info">
                                    <% if (contact.budget) { %>
                                        <span><strong>Budget:</strong> <%= contact.budget %></span>
                                    <% } %>
                                    <% if (contact.membership) { %>
                                        <span><strong>Membership:</strong> <%= contact.membership %></span>
                                    <% } %>
                                    <% if (contact.website) { %>
                                        <span>🔗 <a href="<%= contact.website.startsWith('http') ? contact.website : 'https://' + contact.website %>" target="_blank" style="color: var(--primary);">
                                            <%= contact.website %>
                                        </a></span>
                                    <% } %>
                                </div>
                            <% } %>

                            <div class="message-text">
                                <%= contact.message %>
                            </div>

                            <div class="card-footer">
                                <span class="date">
                                    <%= new Date(contact.createdAt).toLocaleString() %>
                                </span>
                                <div>
                                    <a href="mailto:<%= contact.email %>?subject=Re: <%= encodeURIComponent(contact.subject) %>"
                                       style="background: var(--success); color: white; padding: 10px 20px; border-radius: 10px; text-decoration: none; margin-right: 15px; font-weight: 600;">
                                        Reply
                                    </a>
                                    <a href="/admin/contacts/delete/<%= contact._id %>"
                                       onclick="return confirm('Delete this message?')"
                                       class="delete-btn">
                                        Delete
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="no-items">
                        <i class="fas fa-envelope"></i>
                        <p>No messages yet</p>
                        <p>All incoming contact form submissions will appear here.</p>
                    </div>
                <% } %>
            </div>
        </section>

        <!-- Demo Requests -->
        <section id="demo-requests" class="content-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-phone-volume"></i> Free Demo Requests
                </h2>
                <div class="badge" style="background: var(--warning);">
                    <%= demoRequests?.length || 0 %> requests
                </div>
            </div>

            <div class="grid" style="grid-template-columns: 1fr;">
                <% if (demoRequests && demoRequests.length > 0) { %>
                    <% demoRequests.forEach(req => { %>
                        <div class="item-card contact-card">
                            <div style="display: flex; justify-content: space-between; align-items: center;">
                                <div>
                                    <h4 style="margin: 0;">Mobile: <%= req.mobile %></h4>
                                    <small style="color: var(--text-light);">
                                        Requested on <%= new Date(req.createdAt).toLocaleString() %>
                                    </small>
                                </div>
                                <a href="tel:<%= req.mobile %>" style="background: var(--success); color: white; padding: 12px 24px; border-radius: 50px; text-decoration: none; font-weight: 600;">
                                    Call Now
                                </a>
                            </div>
                            <div class="card-footer" style="margin-top: 20px;">
                                <a href="/admin/demo/delete/<%= req._id %>"
                                   onclick="return confirm('Delete this request?')"
                                   class="delete-btn">
                                    Delete
                                </a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="no-items">
                        <i class="fas fa-phone-volume"></i>
                        <p>No demo requests yet</p>
                        <p>Requests from the "Book Free Demo" button will appear here.</p>
                    </div>
                <% } %>
            </div>
        </section>
    </main>

    <script>
        // Current Date
        const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
        document.getElementById('current-date').textContent = new Date().toLocaleDateString('en-US', options);

        // Smooth scrolling & active nav
        document.querySelectorAll('.nav-item[href^="#"]').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    window.scrollTo({
                        top: target.offsetTop - 100,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Video hover play
        document.querySelectorAll('.thumbnail video').forEach(video => {
            const card = video.closest('.item-card');
            card.addEventListener('mouseenter', () => video.play());
            card.addEventListener('mouseleave', () => {
                video.pause();
                video.currentTime = 0;
            });
        });
    </script>
</body>
</html>